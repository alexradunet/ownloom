FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install --production

COPY dist/ ./dist/

VOLUME /data/auth

ENV BLOOM_CHANNELS_HOST=127.0.0.1
ENV BLOOM_CHANNELS_PORT=18800
ENV BLOOM_AUTH_DIR=/data/auth

CMD ["node", "dist/transport.js"]
