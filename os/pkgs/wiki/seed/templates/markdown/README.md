# Markdown Templates

These templates are plain Markdown.

They assume:
- YAML frontmatter
- stable `id`
- standard Markdown links in the body
- no editor-specific templating features

Replace placeholder values manually or via your tool of choice.
